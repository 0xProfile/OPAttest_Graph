{
  "name": "credence",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ credence",
    "create-local": "graph create --node http://localhost:8020/ credence",
    "remove-local": "graph remove --node http://localhost:8020/ credence",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 credence",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.42.4",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
